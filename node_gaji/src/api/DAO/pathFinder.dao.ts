import { Request, Response } from "express";
import axios from "axios";
import { description } from '../../data/destinationData.json';

const getDestinations = async (_req: Request, res: Response): Promise<void> => {

}